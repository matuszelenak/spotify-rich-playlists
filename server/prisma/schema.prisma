generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model SpotifySongTempo {
  id  String
  tempo Float
  source String @default("extracted")

  @@id(name: "songId", [id, source])
}