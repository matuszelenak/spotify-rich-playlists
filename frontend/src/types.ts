export type TrackRow = {
    index: number,
    name: string,
    artists: string,
    album: string,
    duration: number,
    previewUrl: string | null,
    ourBpm: number | null,
    trackUri: string,
    tempo: number,
    id: string
    albumUrl: string
}
